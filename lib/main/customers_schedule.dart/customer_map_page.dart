import 'package:flutter/material.dart';

class CustomerMap extends StatelessWidget {
  const CustomerMap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'It is a customer map  page.',
        style: TextStyle(fontSize: 35.0),
      ),
    );
  }
}
